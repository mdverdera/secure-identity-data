namespace IdentityData.IntegrationTests;

/// <summary>
/// Placeholder integration tests — real tests are added in Sub-Task 6.
/// This class exists to ensure the project builds and the test runner finds at least one test.
/// </summary>
public sealed class PlaceholderTests
{
    [Fact]
    public void Placeholder_AlwaysPasses()
    {
        // This test exists solely to verify the project compiles and the test runner is wired up.
        Assert.True(true);
    }
}
